# OPENCODE.md - SDL-MCP

Use SDL-MCP as the primary tool surface for repository `{{REPO_ID}}`.

## Required Workflow

1. Start with `sdl.repo.status`.
2. Use `sdl.action.search` for discovery (use `query: "*"` to list all).
3. Use `sdl.manual(query|actions|format)` to load only the relevant reference.
4. Use `sdl.context` for code context retrieval (`contextMode: "precise"` for targeted lookups, `"broad"` for exploration).
5. Use `sdl.workflow` for multi-step operations (runtime execution, data transforms, batch mutations) and `runtimeExecute` for repo-local commands — not for context retrieval.
6. Use `symbolRef` or `symbolRefs` when you know a symbol name but not the canonical `symbolId`.
7. Follow SDL guidance fields such as `nextBestAction`, `fallbackTools`, `fallbackRationale`, and ranked candidates instead of retrying blocked native tools.

## Conditional Enforcement

All SDL-MCP enforcement is **conditional** on the server being active (PID file exists).
When SDL-MCP is not running, all native tools (Read, Bash, Explore) work normally with no restrictions.

## Non-Indexed File Access

- Use `file.read` inside `sdl.workflow` for reading non-indexed files (templates, configs, docs) with minimal overhead.
- Native `Read` is allowed for non-indexed file types even when SDL-MCP is active.
- Only indexed source extensions (.ts, .js, .py, .go, .rs, etc.) are redirected to SDL tools.

### file.read — Token-Efficient Modes

Avoid reading entire files. Use targeted modes:

- **Line range**: `{ "fn": "file.read", "args": { "filePath": "config/app.yaml", "offset": 10, "limit": 20 } }`
- **Search**: `{ "fn": "file.read", "args": { "filePath": "docs/guide.md", "search": "authentication", "searchContext": 3 } }`
- **JSON path**: `{ "fn": "file.read", "args": { "filePath": "package.json", "jsonPath": "dependencies" } }`

Prefer `search` or `jsonPath` over full reads. Use `offset`/`limit` when you know the line range.

## Native Tool Restrictions

- Do not use native `read` for indexed source code.
- Do not use native `bash` for repo-local build, test, lint, or diagnostic commands when SDL runtime is enabled.
- If a permission rule or plugin blocks `read` or `bash`, switch to SDL-MCP immediately.

## Runtime Execution

- Use `runtimeExecute` with `outputMode: "minimal"` (default) for ~50-token responses.
- **Parameters**: Use `args` (string array) or `code` (inline string). There is no `command` field.
- Use `runtimeQueryOutput` with the `artifactHandle` to retrieve output details on demand.
- Always set `timeoutMs` to prevent hangs.

## Version Defaults

- `delta.get`: `fromVersion`/`toVersion` optional — auto-resolves to latest/previous.
- `slice.refresh`: `knownVersion` optional — defaults to slice handle's stored version.

## Recommended Runtimes

- `node`
- `typescript`
- `python`
- `ruby`
- `php`
- `shell`
