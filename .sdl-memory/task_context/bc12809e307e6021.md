---
memoryId: bc12809e307e6021
type: task_context
title: "Code review round 2026-03-22: 7 fixes across 5 files, all execSync eliminated"
tags: [code-review, security, execSync, hardening, yaml-escape]
confidence: 0.95
symbols: []
files: [src/mcp/tools/memory.ts, src/sync/sync.ts, src/memory/file-sync.ts, src/runtime/executor.ts, tests/unit/memory-file-sync.test.ts]
createdAt: 2026-03-22T12:51:35.990Z
deleted: false
---
Second exhaustive code review session. Found and fixed 7 issues across 5 files:

1. **computeContentHash collision** (src/mcp/tools/memory.ts): Hash used `repoId + type + title + content` without delimiters — boundary ambiguity could cause incorrect deduplication. Fixed with null-byte join: `[repoId, type, title, content].join("\0")`.

2. **execSync in sync.ts** (src/sync/sync.ts): `getGitCommitSha`/`getGitBranch` used `execSync` (shell invocation). Fixed to `execFileSync("git", [...args])`. Consistent with prior runtimes.ts fix.

3. **escapeYamlString missing newline escape** (src/memory/file-sync.ts): Newlines in titles would corrupt YAML frontmatter. Added `\n`→`\\n` and `\r`→`\\r` escaping.

4. **unescapeYamlString missing newline unescape** (src/memory/file-sync.ts): Added corresponding `\\n`→`\n` and `\\r`→`\r` for round-trip fidelity. Order: unescape `\n`/`\r` before `\\` to avoid false matches.

5. **updateMemoryFileFrontmatter temp file leak** (src/memory/file-sync.ts): Missing try-catch around rename left orphaned `.tmp` files on failure. Now matches `writeMemoryFile` pattern.

6. **killProcessTree execSync** (src/runtime/executor.ts): Windows `taskkill` used `execSync` with template literal. Fixed to `execFileSync("taskkill", ["/T", "/F", "/PID", String(pid)])`.

7. **Test memoryIds invalid hex** (tests/unit/memory-file-sync.test.ts): 6 tests used non-hex memoryIds (test123, xyz789, etc.) that fail MEMORY_ID_PATTERN validation added in prior session. Updated to valid hex IDs.

**Confirmed clean**: Zero execSync remaining in src/, zero as-any violations (2 documented exceptions in compact-schema.ts), zero @ts-ignore, all imports have .js extensions, no Cypher injection patterns.