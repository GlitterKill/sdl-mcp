---
memoryId: 2542710ee19b470f
type: task_context
title: "Code review round 3 (2026-03-22): 31 fixes across 31 files, all clean"
tags: [code-review, hardening, security, quality, adapters, memory]
confidence: 0.95
symbols: []
files: [src/memory/file-sync.ts, src/indexer/adapter/go.ts, src/indexer/adapter/rust.ts, src/indexer/adapter/cpp.ts, src/indexer/adapter/csharp.ts, src/runtime/executor.ts, src/mcp/hooks/memory-hint.ts, src/agent/planner.ts, src/config/loadConfig.ts, src/util/safeJson.ts, src/db/ladybug-usage.ts, src/db/ladybug-feedback.ts]
createdAt: 2026-03-22T14:46:31.816Z
deleted: false
---
Third exhaustive code review session using 6 parallel review agents + 4 parallel fix agents. Found and fixed 31 issues across 31 files (1 critical, 3 high, ~14 medium, ~13 low). All 3125 tests pass.

**Critical fixes:**
1. `deleteMemoryFile` missing `validateMemoryId` — path traversal defense-in-depth gap (src/memory/file-sync.ts)

**High-severity fixes:**
2. Go adapter `extractCalls` emitted 0-based line numbers (should be 1-based) — caused incorrect caller attribution
3. Rust adapter same issue in 3 locations (macro calls, regular calls, chained calls)
4. `serializeYamlArray` didn't escape commas/brackets — data corruption on round-trip
5. `killProcessTree` setTimeout not `.unref()`'d — prevented clean Node.js shutdown

**Medium-severity fixes:**
6. Go `isExported` marked `_` and digits as exported — fixed with Unicode regex `/^\p{Lu}/u`
7. C# `processAwaitExpression` used `node.firstChild` (returns await keyword) → `firstNamedChild`
8. C++ `extractVisibility` returned first access specifier, not last before target node
9. `extractSymbols.ts` used `console.warn` instead of logger
10. Memory hint hook session eviction deleted sessions before recording current call (ordering bug)
11. Planner budget validation used truthiness check (`if (x && x < 0)`) allowing 0 to bypass
12. `deleteMemoryFile`/`updateMemoryFileFrontmatter` errors silently swallowed with `.catch(() => {})`
13. `typeToDir` default case passed raw type as directory name — now throws
14. `readMemoryFile` caught ALL errors including EACCES — now logs non-ENOENT

**Low-severity fixes (logging, defense-in-depth, type safety):**
15-19. Silent catch blocks replaced with logger calls: blastRadius, sync.ts (2), diagnostics.ts, repo.ts
20. `notifyToolListChanged` now includes error details
21. Prototype pollution defense in `expandEnvVars`
22. `z.record(z.any())` → `z.record(z.unknown())` in safeJson.ts and tools.ts
23. Cypher LIMIT interpolation → parameterized `$limit` in ladybug-usage.ts and ladybug-feedback.ts (6 occurrences)
24. `tsconfig.scripts.json` referenced wrong migration filename
25. `console.warn` → `logger.warn` in go-adapter.ts and php-adapter.ts import resolution
26. Empty catch in executor.ts JSON.parse now logs

**Test fixes:**
27-31. Updated test assertions for changed error messages, invalid memoryIds, overview cache, tmpdir usage

**Confirmed clean:** Zero `execSync`, zero `as any` (except documented eslint-disable), zero `@ts-ignore`, zero `console.warn` in src/, zero bare `catch {}`, all imports have .js extensions, all Cypher uses parameterized queries.