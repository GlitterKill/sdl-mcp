---
memoryId: e24132c9c0432dfd
type: task_context
title: "Code review round 6 (2026-03-24): 26 fixes across 3 rounds, verified clean"
tags: [code-review, security, memory-management, data-integrity, round-6]
confidence: 0.95
symbols: []
files: [src/code-mode/ref-resolver.ts, src/memory/file-sync.ts, src/sync/sync.ts, src/live-index/overlay-reader.ts, src/live-index/overlay-embedding-cache.ts, src/db/ladybug-repos.ts, src/db/ladybug-memory.ts, src/indexer/adapter/csharp.ts, src/indexer/adapter/cpp.ts, src/graph/minHeap.ts, src/delta/blastRadius.ts, src/config/constants.ts, src/util/logger.ts, src/live-index/idle-monitor.ts, src/mcp/tools/slice.ts, src/mcp/tools/runtime.ts, src/cli/commands/init.ts, src/main.ts, src/ts/diagnostics.ts]
createdAt: 2026-03-25T04:10:55.463Z
deleted: false
---
Exhaustive 3-round code review with parallel agent teams. 26 fixes applied across ~18 files, all verified clean with zero regressions.

CRITICAL fixes (2):
- Prototype pollution in ref-resolver.ts: BLOCKED_PROPS denylist for __proto__/constructor/prototype
- Overlay snapshot cache unbounded: MAX_SNAPSHOT_CACHE_SIZE=50 with FIFO eviction in overlay-reader.ts

HIGH fixes (9):
- Overlay embedding cache unbounded: MAX_EMBEDDING_CACHE_ENTRIES=500 in overlay-embedding-cache.ts
- Logger byte counting: Buffer.byteLength instead of line.length in logger.ts
- typeToDir path traversal: validates against /,\,.. in file-sync.ts
- Sync import zip bomb: MAX_DECOMPRESSED_SIZE=100MB in sync.ts
- Concurrency tracker reset: no longer replaced on config change in runtime.ts
- Slice refresh lease mutation: notModified only updates expiresAt in slice.ts
- Shell injection: shellEscape for pidfilePath in init.ts hook scripts
- unhandledRejection: now calls process.exit(1) in main.ts
- deleteRepo: now cleans FileSummary nodes+edges in ladybug-repos.ts

MEDIUM fixes (10):
- C#/C++ adapters: allow partial extraction on parse errors
- C++ findEnclosingSymbol: use shared utility (column bug fix)
- MinHeap toArray→drainSorted rename
- Blast radius normalizedDistance: Math.max(0,...) clamping
- queryMemories: safeJsonParse for tags + LIMIT parameterized
- _deleteFilesByIdsInner: cleans FileSummary on file delete
- deleteRepo: cleans UsageSnapshot nodes
- Logger error swallowing: stderr fallback with rate limiting
- Idle monitor: NaN guard on Date.parse
- DiagnosticsManager: cache bounded to 5 with dispose

LOW fixes (5):
- TOKENS_PER_CHAR_ESTIMATE renamed to CHARS_PER_TOKEN_ESTIMATE
- loadTextTemplate path traversal defense
- typeToDir error message includes type value
- overlay-reader.ts missing logger import added